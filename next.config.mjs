/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};
const   i18n = {
    defaultLocale: "en",
    locales: ["en", "uz"],
  }
export default {
  nextConfig,
  i18n
};
